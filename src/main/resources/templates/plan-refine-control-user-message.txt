<loop-step>
  <name>{loop.name}</name>
  <iterates-over>{loop.over}</iterates-over>
</loop-step>

{#if producer != null}
<producer-step>
  <name>{producer.name}</name>
  <agent>{producer.agentName}</agent>
  <toolkits>{producer.toolkits}</toolkits>
  <instructions>{producer.instructions}</instructions>
</producer-step>
{/if}

<body-stepConfigs>{#for step in bodySteps}
  <step>
    <name>{step.name}</name>
    <agent>{step.agentName}</agent>
    <toolkits>{step.toolkits}</toolkits>
    <instructions>{step.instructions}</instructions>
  </step>{/for}
</body-stepConfigs>

<available-agents>{#for agent in agents}
  <agent>
    <name>{agent.name}</name>
    <role>{agent.role}</role>
  </agent>{/for}
</available-agents>

<tool-definitions>{#for tool in tools}
  <tool>
    <name>{tool.name}</name>
    <description>{tool.description}</description>
    <input-schema>{tool.schema}</input-schema>
  </tool>{/for}
</tool-definitions>
